@page
@model Gestion_Restaurant.Pages.Paiements.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Paiement</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Paiement.MoyenPaiement)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Paiement.MoyenPaiement)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Paiement.Montant)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Paiement.Montant)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-primary" asp-page="./Edit" asp-route-id="@Model.Paiement?.Id">Modifier</a>
    <a class="btn btn-secondary" asp-page="./Index">Retour</a>
</div>